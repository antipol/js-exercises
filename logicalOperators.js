// increment and decrement
let x = 30;
let y = 10;
let val;
// console.log(--x); //29

// the AND operator (&&) is like AND in logic gates, both expressions have to be true in order to return true, otherwise it will be false
// val = x < 100 && y > 200;
// console.log(val); //false

// the OR operator (||) returns true if one or both expressions are true
// val = x == 10 || y == 5; //false
// console.log(val);
// val = x == 30 || y == 7;

// the NOT operator (!) returns TRUE for false statements and FALSE for true statements
// val = x != 30; //false
// val = !(x < 100); //false
